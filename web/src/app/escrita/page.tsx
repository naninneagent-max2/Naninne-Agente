import { EscritaContent } from "./_components/EscritaContent";

export const metadata = {
  title: "Escrita Criativa",
  description: "Escreva livros, capítulos, ensaios e ficção com apoio de IA.",
};

export default function EscritaPage() {
  return <EscritaContent />;
}
